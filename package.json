{
  "dependencies": {
    "express": "^4.19.2",
    "moment": "^2.30.1",
    "mysql": "^2.18.1"
  },
  "scripts": {
    "build": "mkdir dist && cp -r *.php dist/ && cp -r assets dist/ && cp -r style.css dist/"
  }
}
